{
  "name": "geocast-workflow-poc",
  "version": "1.0.0",
  "description": "Chained agentic pipeline on Render Workflows: LLM tasks with a parallel fan-out and typed, JSON-serializable contracts between steps.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@renderinc/sdk": "^0.5.0",
    "openai": "^6.34.0"
  },
  "devDependencies": {
    "tsx": "^4.20.2",
    "typescript": "^6.0.3"
  }
}
