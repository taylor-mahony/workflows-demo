# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# The OpenAI client reads this implicitly via `new OpenAI()`.
OPENAI_API_KEY=

# Open-Meteo (fetchWeather) requires no key.
